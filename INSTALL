VSQLite++ - A welldesigned and portable SQLite3 Wrapper for C++

The build system uses premake4. To generate libraries with g++ run:
`premake4 gmake` in the build sub directory

For other build targets like CodeLite, Visual Studio or XCode check the premake documentation


